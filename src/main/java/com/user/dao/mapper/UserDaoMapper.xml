<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.user.dao.UserDao">
    <resultMap id="userMap" type="User">
        <id property="id" column="id" jdbcType="INTEGER"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
        <result property="salt" column="salt" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="mobile" column="mobile" jdbcType="VARCHAR"/>
        <result property="icon" column="icon" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
    </resultMap>

    <insert id="add" keyProperty="id" useGeneratedKeys="true" parameterType="User">
        insert into la_user(user_name,salt,password,name,email,mobile,icon,status,role_id)
        VALUES(#{userName},#{salt},#{password},#{name},#{email},#{mobile},#{icon},#{status},#{role.id})
    </insert>
    
    <update id="edit">
        update la_user SET
        password = #{password},
        name = #{name},
        email = #{email},
        mobile = #{mobile},
        icon = #{icon},
        where id = #{id}
    </update>

    <update id="delete">
        update la_user set
        status = #{status}
        where id = #{id}
    </update>

    <select id="findById" resultMap="userMap">
        select * from la_user
        <where>
            <if test="id != null">
                id = #{id}
            </if>
        </where>
    </select>

</mapper>